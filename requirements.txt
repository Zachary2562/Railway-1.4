# ─── Build tools ─────────────────────────────────────────────────────────────
setuptools>=65.5.0
wheel

# ─── Core numerical libraries (ensure NumPy + Cython exist before any builds) ──
numpy==1.24.4
Cython>=0.22

# ─── Pure‐Python packages that have wheels for Python 3.11 ─────────────────────
pandas==2.0.3
matplotlib==3.7.1
yfinance==0.2.24
streamlit==1.24.1
scikit-learn==1.3.2
tensorflow-cpu==2.12.0

# ─── Disable PEP 517 isolation for packages that still need NumPy/Cython at build time ───
--no-build-isolation
pystan==2.19.1.1
prophet==1.1.4
ta==0.11.0
